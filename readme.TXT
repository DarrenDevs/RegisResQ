This was a school project designed to introduce us to the MVC (Model, View, Controler)
design pattern. It is a mockup app for a make believe animal rescue. Database is a
MySql server that was run on my localhost, so testing CRUD operations on a different 
machine will require alterations to the database path.